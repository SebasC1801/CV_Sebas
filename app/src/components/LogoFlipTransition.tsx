"use client";

export default function LogoFlipTransition() {
  return (
    <div className="relative z-10 w-full min-h-[50vh] flex flex-col items-center justify-center py-20 bg-[var(--surface)]">
      <div className="p-10 flex items-center justify-center w-[80vw] max-w-md">
        <div className="text-6xl md:text-8xl font-bold font-oswald tracking-widest text-[var(--foreground)] select-none">
          SEBAS
        </div>
      </div>
    </div>
  );
}
